import { BrowserRouter } from "react-router-dom"
import {Routing} from "../src/Routing/Routing.jsx"

export const App = () => {
  return<>

    <BrowserRouter>
    
    <Routing />
    
    </BrowserRouter>
  
  
  </>}